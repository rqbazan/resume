{
  "name": "my-resume-with-react-pdf",
  "version": "0.0.0-semantically-released",
  "author": "Ricardo Q. Bazan <raquirozbazan@gmail.com>",
  "license": "MIT",
  "scripts": {
    "vercel-build": "yarn esbuild --define:process.env.NODE_ENV=\\\"production\\\" ./app --bundle --minify --platform=node --target=node12 --outdir=api/_lib --loader:.js=jsx --loader:.ttf=dataurl",
    "build": "yarn vercel-build"
  },
  "dependencies": {
    "@react-pdf/renderer": "^1.6.12",
    "@react-pdf/styled-components": "^1.4.0",
    "react": "16.12.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.3.5",
    "@commitlint/config-conventional": "^8.3.4",
    "esbuild": "^0.8.31",
    "husky": "^4.2.3"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  }
}
